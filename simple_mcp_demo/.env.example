# Configuration de l'API LLMaaS
# ==============================

# Clé API LLMaaS (OBLIGATOIRE)
# Vous pouvez obtenir votre clé API sur le portail Cloud Temple
API_KEY=votre_cle_api_ici

# URL de l'API LLMaaS (optionnel, valeur par défaut fournie)
API_URL=https://api.ai.cloud-temple.com/v1

# Modèle par défaut (optionnel)
# Pour cet exemple, nous recommandons qwen3-next:80b pour sa performance
DEFAULT_MODEL=qwen3-next:80b

# Configuration du serveur MCP HTTP
# ===================================

# URL du serveur MCP (doit être démarré séparément)
# Le serveur MCP est un service web indépendant accessible via HTTP
MCP_SERVER_URL=http://localhost:8000

# Clé de sécurité pour protéger le serveur MCP (Optionnel mais recommandé)
# Si définie, le client devra fournir cette clé pour se connecter
MCP_SERVER_AUTH_KEY=ma_cle_secrete_super_securisee
